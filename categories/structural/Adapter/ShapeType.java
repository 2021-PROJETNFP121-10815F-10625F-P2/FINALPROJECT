package categories.structural.Adapter;

/**
 * ShapeType Enum.
 *
 * @author  (your name)
 * @version (a version number or a date)
 */
public enum ShapeType {
    CIRCLE,
    RECTANGLE,
    TRIANGLE
}
